---
const { tags } = Astro.props;
---
<ul class="flex flex-row gap-3 justify-center items-center">
    {tags.map(tag =>
            <li class=" border-primary border-2 rounded-full px-2 py-0">
                <a href={`/tags/${tag}`} class="">{tag}</a>
            </li>
    )}
</ul>